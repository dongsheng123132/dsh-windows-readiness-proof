import assert from 'node:assert/strict'
import { spawn } from 'node:child_process'
const child = spawn(process.execPath, ['mcp-server.mjs'], { cwd: process.cwd(), shell: false, stdio: ['pipe', 'pipe', 'inherit'] }); let output = ''; child.stdout.setEncoding('utf8'); child.stdout.on('data', (chunk) => { output += chunk }); child.stdin.write(`${JSON.stringify({ jsonrpc: '2.0', id: 1, method: 'initialize', params: { protocolVersion: '2025-06-18' } })}\n`); child.stdin.write(`${JSON.stringify({ jsonrpc: '2.0', id: 2, method: 'tools/list' })}\n`); child.stdin.end(); await new Promise((resolve, reject) => { child.on('exit', (code) => code === 0 ? resolve() : reject(new Error(`MCP exited ${code}`))); child.on('error', reject) }); const messages = output.trim().split(/\r?\n/).map(JSON.parse); assert.equal(messages[0].result.serverInfo.name, 'dsh-windows-readiness-proof'); assert.deepEqual(messages[1].result.tools.map(({ name }) => name), ['windows_readiness_inspect', 'windows_readiness_verify']); process.stdout.write(`${JSON.stringify({ ok: true, tools: messages[1].result.tools.map(({ name }) => name) })}\n`)
