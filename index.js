import { defineTool } from '@deepseek-ai/dsh-tools'
import { inspectWindowsReadiness, verifyWindowsReadiness } from './lib/windows-readiness-proof.mjs'
export const name = 'dsh-windows-readiness-proof'; export const inject = ['tools']; const renderJson = (_args, value) => [{ type: 'text', text: JSON.stringify(value, null, 2) }]; const base = (config, args) => ({ workspaceRoot: config.workspaceRoot ?? process.cwd(), manifestPath: args.manifestPath, artifactDir: args.artifactDir }); export function createDefinitions(_ctx, config = {}) { return [defineTool({ name: 'dsh_windows_readiness_inspect', description: 'Inspect a pinned managed-Windows readiness policy without collecting or returning host observations.', parameters: { manifestPath: { type: 'string', required: true } }, output: { schema: { type: 'json' }, render: renderJson }, execute(args) { return inspectWindowsReadiness(base(config, args)) } }), defineTool({ name: 'dsh_windows_readiness_verify', description: 'Verify sanitized Windows/DSH observations and emit a content-addressed readiness proof without modifying the host.', parameters: { manifestPath: { type: 'string', required: true }, artifactDir: { type: 'string', required: true } }, output: { schema: { type: 'json' }, render: renderJson }, execute(args) { return verifyWindowsReadiness(base(config, args)) } })] } export function apply(ctx, config = {}) { for (const definition of createDefinitions(ctx, config)) ctx.tools.register(definition) } export default apply
